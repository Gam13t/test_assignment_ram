# Python test assignment

### To run this project:

0. (Preferably) Make a new env by executing:
```python3 -m venv env```
1. Install the requirements by executing:
```pip install -r requirements.txt```
2. Run the app.py by executing:
```python app.py```
